export default {
  INFURA: 'INFURA',
  HTTP: 'HTTP',
  TEST: 'TEST',

  // a window provider is one that comes from the `window` global in the
  // browser, i.e. `window.web3`, or from MetaMask's new "postMessage" method of
  // requesting a provider.
  WINDOW: 'WINDOW'
};
